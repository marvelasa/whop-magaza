{
  "name": "marvelasa-standalone",
  "version": "1.0.0",
  "private": true,
  "description": "MARVELASA storefront — self-hosted frontend + backend that mints real Whop checkout sessions.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5"
  }
}
